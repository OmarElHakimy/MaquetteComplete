the maquette folder has two folders:
the frontend folder:
use the npm install command for installing all package that may apps need
the backend folder:
first of all use npm install command
after that create ".env" file in main directory
- add new values:
	APP_KEYS={random-string-doesn’t-matter}
	API_TOKEN_SALT={random-string-doesn’t-matter}

to connect with strapi use the super admin account:
email: omar-haki@gmail.com
mot de passe: Maquette123